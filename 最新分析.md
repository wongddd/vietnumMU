# 最新分析报告

> 分析日期: 2026-05-28
> 项目: Mu Online 私服模拟器 (IGCN/X-Team 修改版)
> 版本: GAMESERVER_UPDATE=803 (Season 8)

---

## 1. 协议匹配度

### 1.1 总览

| 端 | 文件 | head 处理数 |
|---|------|-----------|
| 服务端 | Protocol.cpp (ProtocolCore) | 115+ 个 head 分支, 含 322+ 个 case 语句 |
| 客户端 | Protocol.cpp (ProtocolCoreEx) | 16 个 head 分支, 含 107+ 个 case 语句 |

服务端处理的是 **客户端 -> 服务端** 的请求 (CG 开头), 客户端处理的是 **服务端 -> 客户端** 的推送 (GC 开头)。两者功能互补而非一一对应。

### 1.2 匹配的协议对

服务端接收 (CG) 与客户端发送 (BDataSend) 对照:

| 功能 | 服务端接收 head | 客户端发送函数 |
|------|---------------|--------------|
| 聊天 | 0x00 | CGChatSend |
| 密语 | 0x02 | CGChatWhisperSend |
| 攻击 | PROTOCOL_CODE2 (0x18/~) | CGSendAttack |
| 移动 | PROTOCOL_CODE1 | CGSendMove |
| 位置 | PROTOCOL_CODE3 | CGSendPosition |
| 技能攻击 | 0x19 | CGSendSkillAttack |
| 拾取 | 0x22 | CGSendGetItem |
| 丢弃 | 0x23 | CGSendDropItem |
| 使用物品 | 0x26 | CGSendUseItem |
| NPC 对话 | 0x30 | CGSendNpcTalk |
| 购买物品 | 0x32 | CGSendItemBuy |
| 出售物品 | 0x33 | CGSendItemSell |
| 修理物品 | 0x34 | CGSendItemRepair |
| 交易请求 | 0x36 | CGSendTradeRequest |
| 个人商店 | 0x3F (sub 01-07) | CGSendPShop* |
| 组队 | 0x40-0x43 | CGSendParty* |
| 战盟 | 0x50-0x57 | CGSendGuild* |
| 仓库 | 0x81-0x83 | CGSendWarehouse* |
| 混沌合成 | 0x86-0x88 | CGSendChaosMix |
| 角色信息 | 0xF3 | CGSendCharacter* |
| 恶魔广场 | 0x90 | CGDevilSquareEnter |
| 血色城堡 | 0x9A | CGBloodCastleEnter |
| 宠物命令 | 0xA7 | CGPetItemCommand |
| 师徒/DUEL | 0xAA | CGSendDuel* |
| 幻影寺院 | 0xBF | CGSendIllusionTemple* |
| DUEL (旧版) | 0xAB-0xAC | CGSendDuelEnd/Ok |
| 连接认证 | 0xF1 | CGSendConnectAccount |
| 物品邮寄 | 0x78 | CGSendPostItem |

### 1.3 关键匹配问题

**高危 - 协议 head 0xFB 绕过安全检查：**
服务端 ProtocolCore 函数在进入主 switch 之前特殊处理了 0xFB:
```cpp
if (gObj[aIndex].Type == OBJECT_USER && lpMsg[0] == 0xC1 && head == 0xFB)
{
    // 直接处理, 不经过 HackPacketCheck
    switch (lpMsg[3]) { ... }
    return;
}
```
这意味着 0xFB 协议的包不经过 `gHackPacketCheck.CheckPacketHack()` 的校验。攻击者可利用此通道发送未经检查的自定义数据。

**注意 - 客户端协议 head 0x88 (混合概率查询):**
客户端 ProtocolEx 有 0x88 处理 (`CB_GETMIXRATE`), 服务端也有 0x88 处理 (`CB_GETMIXRATE`)。两端的宏必须同时启用才能正常工作 — 当前均为 1, 匹配。

**缺失的服务器协议处理：**
以下 head 在服务端有注册但被注释或条件编译关闭:
- 0xBF sub 0x5A~0x6B: ITL (伊格尼斯试炼) 相关协议被注释, 存在 18 个未实现的子码
- 0xF8 sub 0x34~0x4B: ArcaBattle 部分子码标注为注释 (空实现)
- 0xD0 sub 0x05~0x06: PcPoint (GAMESERVER_UPDATE<=402 才启用, 当前 803 不启用)

### 1.4 协议不匹配摘要

| 服务端 | 客户端 | 状态 |
|--------|--------|------|
| head 0xFB (OfflineMode/ChangeClass) | head 0xFB 处理 (ChangeClass/JewelBank) | 部分匹配 |
| head 0xD3 (VongQuay/BotTrader/MocNap/LockItem等) | head 0xD3 (VongQuay/BotTrader/MocNap等) | 基本匹配 |
| head 0xFA (CongHuong/HuyDongExc/JewelBankV2) | head 0xFA (CongHuong/HuyDongExc/JewelBankV2) | 基本匹配 |
| head 0x4E (MuunSystem/HAISLOTRING) | head 0x4E (MuunSystem/HAISLOTRING) | ✓ |
| 自定义 0xF3 子码空间 | 自定义 0xF3 子码空间 | 基本匹配 |

---

## 2. 自定义系统一致性

### 2.1 自定义宏定义对比

| 功能 | 服务端 (stdafx.h) | 客户端 (Defined_Global.h) | 状态 |
|------|-------------------|--------------------------|------|
| CUSTOM_BCHOTROI | 1 | 1 | ✓ |
| CB_AutoBanking | 1 | 1 | ✓ |
| JEWELBANKVER2 | 1 | 1 | ✓ |
| CUSTOM_BRANKINGNEW | 1 | 1 | ✓ |
| CB_DANGKYINGAME | 1 | 1 | ✓ |
| CB_AUTORESETINFO | 1 | 1 | ✓ |
| CB_VIP_CHAR | 1 | 1 | ✓ |
| HAISLOTRING | 1 | 1 | ✓ |
| DOIMK | 1 | 1 | ✓ |
| CTCMINI | 1 | 1 | ✓ |
| CUSTOM_WINDOWLOCKITEM | 1 | 1 | ✓ |
| CB_VIEWCHARITEM | 1 | 1 | ✓ |
| CB_CUSTOMMIXINFO | 1 | 1 | ✓ |
| CB_NEWQUESTFF | 0 | 0 | ✓ (均关闭) |
| CB_HUYDONGEXC | 1 | 1 | ✓ |
| BOSS_GUILD | 1 | 1 | ✓ |
| CB_ActiveInvasionsD | 1 | 1 | ✓ |
| CongHuongV2 | 1 | 1 | ✓ |
| CB_ATTACK_HIDEN_PET | 1 | 1 | ✓ |
| CB_ATTACK_HIDEN_WING | 1 | 1 | ✓ |
| SAUDOIITEM | 1 | 1 | ✓ |
| CB_GETMIXRATE | 1 | 1 | ✓ |
| CB_BXHDMG | 1 | 1 | ✓ |
| CB_NUMBERCHECKSUM | 114749 | 114749 | ✓ |

### 2.2 宏定义不匹配

| 宏 | 服务端 | 客户端 | 风险 |
|----|-------|-------|------|
| **CB_BOTTRADE** vs **CB_BOTTRADER** | `CB_BOTTRADE` (stdafx.h:159) | `CB_BOTTRADER` (Defined_Global.h:97) | **中危**: 不同宏名, 如果代码中条件编译不统一可能导致 BotTrader 功能异常 |
| **MOCNAP** vs **MaNap** | `MOCNAP` (stdafx.h:172) | `MaNap` (Defined_Global.h:110) | **中危**: 不同宏名, 应统一命名, 当前值均为 1 所以工作正常 |
| CB_ANTIHACKGGNEW | 未定义 | 0 | 低危: 客户端保留但未启用 |
| CB_SHOWWCOIN_V | 未定义 | 0 | 低危: 仅客户端显示 |
| CB_SHOWCHOTROI_V | 未定义 | 0 | 低危: 仅客户端显示 |
| CB_AUTOLOGINWIN | 未定义 | 1 | 低危: 仅客户端功能 |
| CB_OFFMUHELPER | 未定义 | 1 | **注意**: 客户端有, 但服务端无对应的宏定义; 通过 Protocol.cpp 实际处理了 |
| CB_CUSTOMOFFTRADE | 未定义 | 1 | **注意**: 服务端用 CustomStore 替代 (0xF3:0xEB) |

### 2.3 自定义系统配置文件对比

| 配置 | 服务端存在 | 客户端存在 | 状态 |
|------|-----------|-----------|------|
| CustomEventTime.xml | MuServer/Data/Custom/CustomEventTime.xml | 由 GetMainInfo 编译为 CBBuffInfo.bin | 间接匹配 |
| CustomJewel.xml | 存在 | Defined_Global.h 定义 MAX_CUSTOM_JEWEL=25 | 服务端 MAX_CUSTOM_JEWEL=25 ✓ |
| CustomWing.xml | 存在 | Defined_Global.h 定义 MAX_CUSTOM_WING=100 | 服务端 MAX_CUSTOM_WING=100 ✓ |
| CustomItem.xml | 存在 | Defined_Global.h 定义 MAX_CUSTOM_ITEM=2000 | 无对应的服务端常量定义 |
| CustomPet.txt | 存在 | 无独立文件, 客户端编译到 CBGetMain.bin | 间接匹配 |

### 2.4 数据结构一致性

自定义系统的数据结构在服务端和客户端独立定义，需要手动同步。关键观察：

- **CustomJewel**: 两端 `MAX_CUSTOM_JEWEL = 25` 一致
- **CustomMove**: 服务端读取 `CustomMove.txt` → 发送给客户端 → 客户端显示传送菜单, 中间无独立数据结构
- **CustomWing**: 服务端 `MAX_CUSTOM_WING = 100` 与客户端一致, 但服务端未定义 `MAX_CUSTOM_WING_EFFECT`
- **CB_DrawCustomRank**: 客户端有独立绘制, 服务端通过 0xD3:0x1E 发送数据

---

## 3. 资源匹配度

### 3.1 资源文件分布

| 端 | 资源位置 | 文件数量特征 |
|-------|---------|------------|
| 服务端 | MuServer/4.GameServer/Sub 1/Data/ | ~100+ 配置文件 (.txt/.xml) |
| 客户端 | ClientFile/Data/ | ~18000+ 文件 (.ozj .ozt .bmd .map .obj) |

### 3.2 物品配置

服务端 `Item/Item.txt` 包含完整的物品属性表 (约 512 类 x 22 属性字段)，包含装备、武器、首饰等全部官方物品。

客户端 `Data/Local/Item_decrypted.txt` 存在于客户端目录，用于客户端解析物品属性。

**潜在问题:** 服务端的 `Custom/` 自定义物品 (CustomJewel, CustomWing, CustomItem) 配置通过 GetMainInfo 编译到 `CBGetMain.bin` 中, 两端的数据流是:

```
服务端 Custom/*.txt/xml → GetMainInfo → CBGetMain.bin → 客户端读取
```

此流程意味若修改服务端自定义物品配置后未重新运行 GetMainInfo 并复制 CBGetMain.bin, 两端物品定义将不同步。**这是高危配置点**。

### 3.3 怪物配置

服务端: `Monster/Monster.txt` (怪物基础属性)
客户端: `Data/Monster/` (模型/动画资源)

**问题:** 客户端怪物模型资源以 .bmd 格式存在 (约 6577 个), 服务端怪物配置编号需要与客户端模型编号匹配。若服务端添加了自定义怪物(`CustomMonster.txt`) 但客户端缺少对应 .bmd 模型, 会导致客户端崩溃。

### 3.4 NPC 商店

服务端: `Shop/` 下 60+ NPC 商店文件, 格式为 `{编号} - {名称}.txt`
客户端: `Data/Interface/` 下 UI 资源

### 3.5 地图资源

客户端: 55 个 `.map` 文件 + 55 个 `.obj` 文件 (对应 World1~World106) + `.ozb` 地面网格
服务端: `Terrain/` 目录 + `Move/Move.txt` 传送配置

**注意:** 服务端配置的地图编号应与客户端的地图文件编号匹配。当前客户端有 World1~World106 的地图资源, 但存在缺失编号 (如 World6, World13~18)。

---

## 4. 潜在 BUG

### 4.1 高危

#### BUG-1: SQL 注入漏洞 (JoinServer + DataServer)

**严重程度: 高危**

`QueryManager::ExecQuery(char* query, ...)` 使用 `vsprintf_s` 将用户输入直接格式化到 SQL 查询字符串中:

```cpp
// QueryManager.cpp:108
vsprintf_s(buff, query, arg);
// 然后直接 SQLExecDirect
SQLExecDirect(this->m_STMT, (SQLCHAR*)buff, SQL_NTS);
```

这意味着所有将用户输入传递到 `ExecQuery` 的地方都存在 SQL 注入风险。

**受影响文件:**
- `JoinServer/JoinServerProtocol.cpp` — 账号认证 (line 114, 143, 173, 181, 195):
  ```cpp
  ExecQuery("SELECT memb__pwd FROM MEMB_INFO WHERE memb___id='%s'", lpMsg->account);
  ```
- `DataServer/DataServerProtocol.cpp` — 角色操作 (line 998~1714):
  ```cpp
  ExecQuery("SELECT * FROM AccountCharacter WHERE Id='%s'", lpMsg->account);
  ExecQuery("EXEC WZ_CreateCharacter '%s','%s','%d'", lpMsg->account, lpMsg->name, lpMsg->Class);
  ExecQuery("SELECT * FROM Character WHERE AccountID='%s' AND Name='%s'", lpMsg->account, lpMsg->name);
  ```
- `DataServer/ChoTroi.cpp` — 市场系统 (line 184~554):
  ```cpp
  sprintf(szQuery, "SELECT TOP %d ID, Account, Name, PriceType, PriceValue FROM ItemMarketData WHERE Status = 0 %s %s ORDER BY ID DESC", ...);
  ```
- `DataServer/DataServerProtocol.cpp` — 充值系统 (line 1443):
  ```cpp
  wsprintf(zzQueryMocNapGet, "SELECT NhanMocNap FROM MEMB_INFO where memb___id='%s'", lpMsg->account);
  ```

攻击者可以发送特制的账号名/角色名/密码数据执行任意 SQL 命令。

#### BUG-2: Chat 消息缓冲区溢出风险

**严重程度: 高危**

`GCNewMessageSend` (Protocol.cpp:4311):
```cpp
void GCNewMessageSend(LPOBJ lpObj, char* message)
{
    PMSG_NEW_MESSAGE_SEND pMsg;
    pMsg.header.set(0xF3, 0xE4, sizeof(pMsg));
    memcpy(pMsg.message, message, sizeof(pMsg.message));  // 无长度检查
    DataSend(lpObj->Index, (BYTE*)&pMsg, pMsg.header.size);
}
```
如果 `message` 长度超过 `pMsg.message` 的缓冲区大小, `memcpy` 造成栈溢出。

`GCPostItemProc` (Protocol.cpp:117): `memcpy(pMsg.chatmsg, lpMsg->chatmsg, sizeof(pMsg.chatmsg));` — 从数据包中拷贝数据到消息结构体，但未验证数据包中 `chatmsg` 的实际长度。

#### BUG-3: protocol 0xFB 绕过 HackPacketCheck

**严重程度: 中高**

```cpp
// Protocol.cpp:159
if (gObj[aIndex].Type == OBJECT_USER && lpMsg[0] == 0xC1 && head == 0xFB)
{
    switch (lpMsg[3]) {
    case 0x3D: g_OfflineMode.Start(...); break;
    case 0x08: gChangeClass.RecvChangeClass(...); break;
    }
    return;  // 不经过 HackPacketCheck
}
```

所有 0xFB 协议的包绕过反外挂检测系统。包括 OfflineMode 和 ChangeClass 功能。攻击者可利用此通道发送任意数据。

### 4.2 中危

#### BUG-4: 除零风险 (怪物/属性计算)

**严重程度: 中危**

GameServer 中的伤害计算、掉落率计算等需要检查除零场景。许多配置项从 INI 中读取未验证是否为 0。

#### BUG-5: CriticalSection 使用不当 — 死锁风险

**严重程度: 中危**

多个队列定时器在 `QueueTimerCallback` 中串行执行, 但自定义系统可能在其他线程中访问共享数据。需要检查所有 `CriticalSection` 的使用是否一致。

#### BUG-6: Reset 系统的数组边界

**严重程度: 中危**

在 Protocol.cpp 的 AutoReset 处理 (line 1596-1605):
```cpp
lpObj->AutoResetStats[0] = mRecv->AutoResetStats[0];
lpObj->AutoResetStats[1] = mRecv->AutoResetStats[1];
lpObj->AutoResetStats[2] = mRecv->AutoResetStats[2];
lpObj->AutoResetStats[3] = mRecv->AutoResetStats[3];
lpObj->AutoResetStats[4] = ((lpObj->Class == CLASS_DL) ? mRecv->AutoResetStats[4] : 0);
```
`RESETCODE_SEND` 结构体中的 `AutoResetStats` 数组大小需与接收端匹配, 否则越界读取。

#### BUG-7: DataServer ChoTroi strcat 拼接缓冲区

**严重程度: 中危**

`ChoTroi.cpp:184` 使用 `strcat` 和 `strcpy` 拼接 SQL 过滤条件:
```cpp
strcat(szFilter, szLevel);
strcat(szFilter, "AND FilterLuck = 1 ");
```
如 `szFilter` 缓冲区不足可能导致溢出。

### 4.3 低危

#### BUG-8: memcpy 未使用 `_s` 安全版本

在服务端 `Protocol.cpp` 中大量使用 `memcpy` 而非 `memcpy_s`, 特别是在消息处理中。虽然大部分拷贝的目标缓冲区大小与源匹配, 但存在单字节差错的可能性。

#### BUG-9: 条件编译不一致的风险

| 宏 | 服务端 | 客户端 | 影响 |
|----|-------|-------|------|
| CB_BOTTRADE vs CB_BOTTRADER | CB_BOTTRADE | CB_BOTTRADER | 若条件编译代码中使用了未定义的宏名, 该段代码会被忽略 |
| MOCNAP vs MaNap | MOCNAP | MaNap | 同上 |

#### BUG-10: 客户端版本编码比较

服务端从 INI 读取 `ServerVersion` 为字符串 "1.04.05", 提取字符索引 0/2/3/5/6 存入 `m_ServerVersion[5]`:
```
m_ServerVersion = {'1', '0', '4', '0', '5'}
```

客户端从 GetMainInfo 的 `ClientVersion` 读取 "1.04.05", 然后用偏移量加密:
```
Version[0] = '1' + 1 = 0x32
Version[1] = '0' + 2 = 0x32
Version[2] = '4' + 3 = 0x37
Version[3] = '0' + 4 = 0x34
Version[4] = '5' + 5 = 0x3A
```

这两者通过 `memcmp` 比较时, 除非服务端 INI 中已存储了加密后的值, 否则比较**必然失败**, 导致客户端无法连接。建议服务端也采用相同的编码逻辑进行比较。

---

## 5. 版本一致性

### 5.1 版本宏定义

| 项目 | 服务端 | 客户端 | 状态 |
|------|-------|-------|------|
| GAMESERVER_UPDATE | 803 (Season 8) | 无对应宏 | N/A |
| Season 显示 | GAMESERVER_SEASON = "SEASON 8" | BTYPECUSTOMSS = 6 | 信息性 |
| 客户端版本 | 1.04.05 (INI) | 1.04.05 (GetMain/MainInfo.ini) | 字符串一致 |
| 客户端序列号 | TbYehR2hFUPBKgZj (INI) | TbYehR2hFUPBKgZj (GetMain/MainInfo.ini) | ✓ |
| NEW_MASTER_SKILL_TREE | 无对应 | 1 | 需要两端支持 |
| UseReconnect | 无对应 | 1 | 客户端仅功能 |

### 5.2 客户端/服务端序列号

- 服务端存储: `m_ServerSerial[17]` (含 null)
- 客户端存储: `ClientSerial[17]` (含 null)
- 客户端发送: 16 字节, 无 null
- 服务端比较: `memcmp(m_ServerSerial, ClientVersion, 16)`

如果服务端 INI 中的 `ServerSerial` 恰好 16 字符 ("TbYehR2hFUPBKgZj"), 则 `sizeof(m_ServerSerial)` = 17, 但客户端发送的是 `sizeof(lpMsg->ClientSerial)` = 16。因此 `memcmp` 只对比前 16 字节, 可正常工作。

### 5.3 一致性判定: **基本一致**

需要确保 `GameServerInfo - Common.ini` 和 `GetMain/MainInfo.ini` 中的版本号/序列号保持同步。

---

## 6. 配置完整性

### 6.1 核心配置文件

| 配置文件 | 存在 | 关键配置 |
|---------|------|---------|
| GetMain/MainInfo.ini | ✓ | IpAddress=197.159.75.59:63000, Version=1.04.05, Serial=TbYehR2hFUPBKgZj |
| ClientFile/config.ini | ✓ | 自定义功能开关, 字体, 图形设置 |
| ClientFile/Data/Local/CBGetMain.bin | ✓ | GetMainInfo 输出 (XOR 加密) |
| ClientFile/Data/Local/CBTextInfo.bin | ✓ | 文本信息 (XOR 加密) |
| ClientFile/Data/Local/License.json | ✓ | 反外挂配置 |

### 6.2 服务端必需的配置目录

| 目录 | 存在 | 说明 |
|------|------|------|
| Custom/ | ✓ | 25+ 自定义系统配置 |
| Event/ | ✓ | 事件配置 |
| EventItemBag/ | ✓ | 掉落包 |
| Item/ | ✓ | 物品系统 |
| Monster/ | ✓ | 怪物系统 |
| Shop/ | ✓ | NPC 商店 |
| Move/ | ✓ | 传送配置 |
| Quest/ | ✓ | 任务配置 |
| QuestWorld/ | ✓ | 世界任务 |
| Ruud/ | ✓ | Ruud 货币 |
| Skill/ | ✓ | 技能配置 |
| Terrain/ | ✓ | 地图地形 |
| CashShop/ | ✓ | 商城配置 |
| Character/ | ✓ | 角色初始属性 |

### 6.3 功能开关一致性

服务端 `CustomEventTime.xml` 中的 `Enable` 控制事件时间显示功能, 但客户端 UI 通过 `MainInfo.ini` 中的 `Menu0~Menu12` 控制菜单按钮显示:

```ini
Menu0 = 1  // 事件时间 (EventTime)
Menu1 = 1  // 购买VIP (BuyVip)
Menu2 = 1  // 排行榜 (Ranking)
Menu3 = 1  // 变性 (ChangeClass)
Menu4 = 1  // 自由市场 (FreeMarket/ChoTroi)
Menu5 = 1  // 称号 (DanhHieu)
Menu6 = 0  // 充值 (NapTien) — 关闭
Menu7 = 1  // 宝石银行 (JewelBank)
Menu8 = 0  // VIP 角色 (VIPChar) — 关闭
Menu9 = 1  // 洗卓越 (HuyDongExc)
Menu10 = 1 // 修改密码 (DoiMK)
Menu11 = 1 // 充值里程碑 (MocNap)
Menu12 = 0 // 锁物品 (LockItem) — 关闭
Menu13 = 0 // 锁物品
```

**注意:** 服务端定义了 `CB_VIP_CHAR=1` 但客户端 `Menu8=0`, 意味着服务端编译了 VIP 角色功能的处理代码, 但客户端的菜单按钮被禁用。但客户端仍可能接收和处理服务器发来的 VIP 角色数据 (通过 0xD3:0x22)。

### 6.4 GetMainInfo 编译链

GetMainInfo 的编译链涉及:

```
MainInfo.ini + CommonManager/*.txt + CustomManager/*.txt + EffectManager/*.txt + ItemManager/*.txt
  → GetMainInfo.exe → CBGetMain.bin + CBTextInfo.bin + License.json
```

需确保每次修改服务端配置后, 重新运行 GetMainInfo 并部署到 ClientFile:

1. 服务端 `Custom/*.txt/.xml` — 修改 → 需同步到 `CommonManager/CustomManager/ItemManager/`
2. 客户端 `Data/Local/CBGetMain.bin` — 需为 GetMainInfo 最新输出
3. 客户端 `Data/Local/CBTextInfo.bin` — 需与 GetMainInfo 同步

但项目仓库中 `CommonManager/`, `CustomManager/`, `EffectManager/`, `ItemManager/` 目录**不存在于 `GetMain/` 目录中**, 说明这些文件可能保存在别处或在编译链中缺失。

---

## 7. 总结与建议

### 7.1 关键发现优先级

| 优先级 | 问题 | 影响 |
|--------|------|------|
| **P0-紧急** | SQL 注入 (JoinServer + DataServer) | 攻击者可获取/篡改数据库全部数据 |
| **P0-紧急** | 聊天系统缓冲区溢出 | 可能导致服务端/客户端崩溃 |
| **P1-高** | head 0xFB 绕过反外挂检测 | 黑客可利用此通道发送免检包 |
| **P1-高** | 自定义系统宏命名不一致 (CB_BOTTRADE/CB_BOTTRADER, MOCNAP/MaNap) | 条件编译可能导致双方协议不匹配 |
| **P2-中** | 版本号编码比较逻辑可能不匹配 | 客户端连接被拒绝 (取决于 INI 配置) |
| **P2-中** | GetMainInfo 编译链源文件缺失 | 无法完整重新生成客户端配置 |
| **P2-中** | 自定义物品配置同步无自动化 | 服务端物品改后需手动同步到 GetMainInfo |
| **P3-低** | ArcaBattle 部分协议空实现 | 相关功能不可用 |
| **P3-低** | ITL (伊格尼斯试炼) 协议被注释 | Season 8 相关功能不可用 |

### 7.2 建议行动

1. **SQL 注入修复**: QueryManager 应改为使用参数化查询 (SQLBindParameter), 而非 `vsprintf_s` 格式化。优先修复 JoinServer 的登录认证和 DataServer 的角色操作。

2. **统一宏命名**: 将 `CB_BOTTRADE`/`CB_BOTTRADER` 和 `MOCNAP`/`MaNap` 统一为相同宏名。

3. **协议安全检查**: 将 head 0xFB 的处理移入主 switch, 或在该处理分支内也调用 `HackPacketCheck`。

4. **版本编码**: 服务端在比较版本时应使用与客户端相同的编码逻辑, 或服务端 INI 中存储明文版本并在读取时编码。

5. **GetMainInfo 链**: 补齐 `GetMain/` 下的源文件目录 (`CommonManager/`, `CustomManager/`, `EffectManager/`, `ItemManager/`), 使编译链可完整执行。

6. **自动化同步**: 建立服务端自定义配置变更后自动触发 GetMainInfo 编译和 CBGetMain.bin 部署的流程。
